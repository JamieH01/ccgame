name: HTML Generation
run-name: HTML files are being generated
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        uses: actions/checkout@v4 
      - run: cargo run && git add . && git commit -m "auto html build" && git push 

